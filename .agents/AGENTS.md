# Workspace Rules & Guidelines

- **Git Push Policy**: After making code changes, ALWAYS keep changes local first and ask the user for explicit approval before committing or pushing to Git (`origin/main`). Never push changes automatically without the user's explicit confirmation.
